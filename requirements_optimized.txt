# Optimized requirements for 2-core, 8GB RAM VM
# Core Flask components
Flask>=2.3.0,<3.0.0
Flask-SocketIO>=5.3.0,<6.0.0

# Async handling (choose one for memory efficiency)
eventlet>=0.33.0,<0.35.0

# Ollama client
ollama>=0.1.0

# Document processing (lightweight)
pypdf>=3.0.0,<4.0.0

# Vector search and embeddings (CPU-optimized versions)
sentence-transformers>=2.2.2,<3.0.0
faiss-cpu>=1.7.4,<2.0.0  # CPU version is lighter than GPU
numpy>=1.23.0,<2.0.0

# Environment management
python-dotenv>=1.0.0

# Production server (memory-efficient)
gunicorn>=21.2.0

# System monitoring
psutil>=5.9.0  # For memory monitoring

# Optional: Advanced logging (lightweight)
loguru>=0.7.0
