# PSK Bot — Cross-platform requirements
# Works on: Windows, macOS (ARM64/x86_64), Ubuntu/Debian (x86_64), other Linux
# Updated: 2026-02-17

# ── Core Web Framework ──────────────────────────────────────────────────
Flask>=3.1,<4
Flask-Cors>=4.0,<5
Flask-SocketIO>=5.5,<6
flask-swagger-ui>=4.11,<5
Werkzeug>=3.1,<4
Jinja2>=3.1,<4
MarkupSafe>=3.0,<4
click>=8.1,<9
itsdangerous>=2.2,<3
blinker>=1.9,<2

# ── Async / WebSocket ──────────────────────────────────────────────────
# gevent is used for production deployment on Unix; not needed on Windows
gevent>=24.0; sys_platform != "win32"
gevent-websocket>=0.10; sys_platform != "win32"
greenlet>=3.0; sys_platform != "win32"
simple-websocket>=1.1,<2
wsproto>=1.2,<2
h11>=0.14,<1
python-engineio>=4.9,<5
python-socketio>=5.11,<6

# ── Server ─────────────────────────────────────────────────────────────
# gunicorn is Unix-only; waitress is the Windows-compatible alternative
gunicorn>=22.0,<24; sys_platform != "win32"
waitress>=3.0; sys_platform == "win32"

# ── ML / AI — relaxed pins for cross-platform binary wheel availability ─
torch>=2.1
faiss-cpu>=1.7
sentence-transformers>=3.0
transformers>=4.40
tokenizers>=0.19
safetensors>=0.4
einops>=0.7

# ── Numeric / Scientific ──────────────────────────────────────────────
numpy>=1.26,<2
scipy>=1.12
scikit-learn>=1.4
sympy>=1.13
mpmath>=1.3

# ── Hugging Face Hub ──────────────────────────────────────────────────
huggingface_hub>=0.23
hf-xet>=1.0; platform_machine != "aarch64" and sys_platform != "win32"
filelock>=3.13
fsspec>=2024.2

# ── LangChain ─────────────────────────────────────────────────────────
langchain-core>=1.0
langchain-text-splitters>=0.3
langsmith>=0.7

# ── HTTP / Networking ─────────────────────────────────────────────────
requests>=2.31,<3
requests-toolbelt>=1.0
httpx>=0.27
httpcore>=1.0
anyio>=4.4
certifi>=2024.1
charset-normalizer>=3.3
idna>=3.6
urllib3>=2.2,<3

# ── Document Parsing ─────────────────────────────────────────────────
beautifulsoup4>=4.12
soupsieve>=2.5
lxml>=5.0
pypdf>=4.0
python-docx>=1.1

# ── Ollama Client ────────────────────────────────────────────────────
ollama>=0.4

# ── Pydantic / Serialization ────────────────────────────────────────
pydantic>=2.7
pydantic_core>=2.18
annotated-types>=0.6
orjson>=3.10
jsonpatch>=1.33
jsonpointer>=3.0

# ── Images ───────────────────────────────────────────────────────────
Pillow>=10.0
networkx>=3.2

# ── Utilities ────────────────────────────────────────────────────────
psutil>=5.9
python-dotenv>=1.0
PyYAML>=6.0
regex>=2024.1
tqdm>=4.66
packaging>=23.0
tenacity>=9.0
joblib>=1.3
threadpoolctl>=3.4
typing_extensions>=4.9
typing-inspection>=0.4
uuid_utils>=0.9
xxhash>=3.4
zstandard>=0.22

# ── zope (gevent dependency, Unix only) ─────────────────────────────
zope.event>=5.0; sys_platform != "win32"
zope.interface>=6.0; sys_platform != "win32"
